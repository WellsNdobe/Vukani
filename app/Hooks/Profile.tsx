import React from 'react';
import { Text, View } from 'react-native';

const Profile: React.FC = () => {
    return (
        <View>
            <Text>This is the Profile hook component.</Text>
        </View>
    );
};

export default Profile;